import ProductForm from "./pricingFormZOD";

// const formResults = ProductForm();

function ResultsDisplay() {
    const formResults = ProductForm()
  return (
    <div>
        <p {formResults.} />
    </div>
  )
}
ResultsDisplay();
